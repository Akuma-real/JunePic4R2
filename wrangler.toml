name = "junepic4r2"
compatibility_date = "2025-01-06"
pages_build_output_dir = "out"

# 最小化配置：仅用于本地 dev 绑定与 Pages 输出目录。
# 生产环境的环境变量与绑定请在 Cloudflare Pages Dashboard 配置，
# 本文件不再包含任何 [vars]（避免在构建日志中显示占位值）。

[[d1_databases]]
binding = "DB"              # 本地 wrangler pages dev 使用；线上请在 Dashboard 绑定
database_name = "junepic_db"
database_id = "281975b0-090d-4d56-ab40-da68672d9c2a"

[[r2_buckets]]
binding = "R2_BUCKET"       # 本地 wrangler pages dev 使用；线上请在 Dashboard 绑定
bucket_name = "pic"
